---
title: "Beads - Box - Ambiguity - Counting"
author: "Kristoffer Klevjer"
date: "2/5/2020"
output: 
    ioslides_presentation:
      logo: logo.png
---

## Code highlighting | Subtitle in header
It is possible to select subsets of code for additional emphasis by adding a special “highlight” comment around the code. For example:
But it doesn't work


## Background images {data-background=logo.png data-background-size=cover}
Can be added by adding {data-background=foo.png data-background-size=cover} directly behind header
Can be added by adding #{data-background=foo.png data-background-size=cover}# directly behind header


## Display modes
The following single character keyboard shortcuts enable alternate display modes:

- 'f': enable fullscreen mode
- 'w': toggle widescreen mode
- 'o': enable overview mode
- 'h': enable code highlight mode
- 'p': show presenter notes (add notes by adding <div class="notes"> This is my note </div>)
- 'esc': exits all modes.
